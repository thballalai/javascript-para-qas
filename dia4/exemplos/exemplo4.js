// Exemplo Prático 4
// Substituindo palavras em uma mensagem

let aviso = "A senha está incorreta";
let novoAviso = aviso.replace("incorreta", "inválida");
console.log(novoAviso);

// Saída: A senha está inválida